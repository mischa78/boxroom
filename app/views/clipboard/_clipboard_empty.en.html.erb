The clipboard is empty. Go <%= link_to 'back', '#', :class => 'back_link' %> and use the
'Add to clipboard' button (<%= image_tag('clipboard_add.png', :alt => 'Add to clipboard') %>)
to add files or folders to the clipboard.
