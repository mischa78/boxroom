<div id="header">
	<% if signed_in? -%>
	<div class="user_welcome">
		<p>
			Hello, <%= current_user.name %>!<br>
			<%= link_to 'Settings', edit_user_path(current_user) %> | <%= link_to 'Sign out', signout_path %>
		</p>
	</div>
	<% end -%>
	<%= link_to image_tag('logo.png', :alt => 'Boxroom'), root_path %>
</div>