<%= bootstrap_form_for @group do |f| %>
  <%= f.error_messages %>
  <%= f.text_field :name, label: t(:name) %>
  <%= f.primary t(:save) %> &nbsp; &mdash; &nbsp;
  <%= link_to t(:back), groups_url %>
<% end %>
