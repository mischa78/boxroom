$('#errorExplanation').remove();

<% if @file.valid? -%>
  exists = false;
<% else -%>
  exists = true;
  $('form').prepend('<%= j error_messages_for(@file) %>');
  $('#user_file_attachment').prop('disabled', false);
<% end -%>
